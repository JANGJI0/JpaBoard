<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>회원 목록</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-white">
  <div class="container mt-5">

    <!-- 로그인 정보 영역 -->
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h2>👥 멤버 목록</h2>
      <div>
        <span>{{loginMember.memberId}}님 환영합니다!</span>
        <a href="/" class="btn btn-outline-light btn-sm ms-2">로그아웃</a>
        <a href="/member/myPage" class="btn btn-outline-warning btn-sm ms-2">🏠</a>
        <a href="/member/removeMember" class="btn btn-outline-danger btn-sm ms-2">회원 탈퇴</a>
      </div>
    </div>

    <!-- 카드형 회원 목록 -->
    <div class="row">
      {{#list.content}}
      <div class="col-md-4 mb-4">
        <div class="card text-dark h-100 shadow-sm border-warning">
          <div class="card-body text-center">
            <h5 class="card-title">회원번호: {{memberNo}}</h5>
            <p class="card-text">아이디: <strong>{{memberId}}</strong></p>
          </div>
        </div>
      </div>
      {{/list.content}}
    </div>

    <!-- 페이징 -->
    <div class="text-center mt-4">
    <!-- 처음으로 -->
	  {{^list.isFirst}}
	  <a href="?currentPage=0&word={{word}}" class="btn btn-outline-secondary me-2">⏮</a>
	  {{/list.isFirst}}
      {{^list.first}}
        <a href="?currentPage={{prePage}}&rowPerPage=3&word={{word}}" class="btn btn-outline-secondary me-2">이전</a>
      {{/list.first}}

      <span>{{currentPage}} / {{TotalPage}}</span>

      {{#list.hasNext}}
        <a href="?currentPage={{nextPage}}&rowPerPage=3&word={{word}}" class="btn btn-outline-secondary ms-2">다음</a>
      {{/list.hasNext}}
      
      <!-- 마지막으로 -->
	  {{#list.hasNext}}
	  <a href="?currentPage={{lastPage}}&word={{word}}" class="btn btn-outline-secondary ms-2">⏭</a>
	  {{/list.hasNext}}
    </div>

  </div>
</body>
</html>