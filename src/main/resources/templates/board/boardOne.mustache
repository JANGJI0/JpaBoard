<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>수정/삭제 페이지</title>
<title>Insert title here</title>
<!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>
{{>layouts/header}}
<!-- ✅ 중앙 정렬 + 너비 제한 -->
<div class="container mt-5 d-flex justify-content-center">
  <div class="p-4 rounded-3 shadow bg-white w-100" style="max-width: 720px;">
	{{#msg}}
		<div class="alert alert-warning text-center">
			{{msg}}
		</div>
	{{/msg}}

<table class="table">
    <thead>
    <tr>
        <th scope="col">no</th>
        <th scope="col">title</th>
        <th scope="col">content</th>
    </tr>
    </thead>
    <tbody>
    {{#board}}
    <tr>
        <th>{{no}}</th>
        <td>{{title}}</td>
        <td>{{content}}</td>
    </tr>
    {{/board}}
    </tbody>
</table>
		<div class="text-center mt-4">
		<a href="/board/modify?no={{board.no}}" class="btn btn-primary me-2">✏️ 수정</a>
		<a href="/board/boardList" class="btn btn-secondary">📚 목록</a>
		 <a href="/board/delete?no={{board.no}}" class="btn btn-danger me-2">🧽 삭제</a>
		</div>
	</div>
</div>
{{>layouts/footer}}
</body>
</html>