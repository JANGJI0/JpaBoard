<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
<!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
   
    /* 전체 원형 메뉴 감싸는 컨테이너 */
    .circle-menu {
      position: relative;
      width: 300px;
      height: 300px;
      margin: 80px auto;
      border-radius: 50%;
    }

    /* 각 메뉴 버튼 스타일 */
	.circle-menu a {
	  position: absolute;
	  width: 120px;
	  height: 120px;
	  border-radius: 50%;
	  background: rgba(255, 255, 255, 0.1);
	  backdrop-filter: blur(8px);
	  border: 2px solid #00f7ff;
	  color: white;
	  text-decoration: none;
	  font-weight: bold;
	  font-size: 1rem;
	  transition: transform 0.3s, box-shadow 0.3s;
	  box-shadow: 0 0 10px #00f7ff, 0 0 40px #00f7ff inset;
	
	  display: flex;
	  flex-direction: column;
	  align-items: center;
	  justify-content: center; /* 세로 가운데 정렬 핵심 */
	  padding: 0; /* 여백 제거 */
	  overflow: hidden; /* 혹시 넘치는 요소 잘라내기 */
	}
	
	.emoji {
	  font-size: 1.7rem;
	  margin-bottom: 4px;
	  line-height: 1; /* 줄 간격 줄이기 */
	}
	
	.label {
	  font-size: 0.8rem;
	  line-height: 1;
	  margin: 0; /* 여백 제거 */
	}

    /* 마우스 오버 시 색상 진하게 */
    .circle-menu a:hover {
	 transform: scale(1.08);
	  box-shadow: 0 0 15px rgba(0, 255, 255, 0.4);
	}

    /* 각 메뉴의 위치를 회전시켜 배치 (수동 위치 지정) */
    .menu1 { top: 0px; left: 100px; } /*위*/
    .menu2 { top: 200px; left: 100px; } /* 아래*/
    .menu3 { top: 100px; left: 0px; } /* 왼쪽*/
    .menu4 { top: 100px; left: 200px; } /* 오른쪽*/

  </style>
</head>
<body class="bg-dark text-white">
	<div class="circle-menu">
		<a href="/member/login" class="menu1">
			 <div class="emoji">🔐</div>
	 		 <div class="label">로그인</div>
		</a>
		<a href="/member/joinMember" class="menu2">
			 <div class="emoji">📝</div>
	 		 <div class="label">회원가입</div>
		</a>
		<a href="/board/boardList" class="menu3">
			 <div class="emoji">🧑‍🏫</div>
			 <div class="label">Board</div>
		</a>
		<a href="/articles/index" class="menu4">
			<div class="emoji">🧾</div>
			<div class="label">Aticles</div>
		</a>
	</div>
</body>
</html>